# 环境变量配置
# 复制此文件为 .env 并填写实际值

# ========================================
# Supabase 配置
# ========================================

# Supabase 项目 URL
# 从 Project Settings > API > Project URL 获取
SUPABASE_URL=https://your-project.supabase.co

# Supabase 匿名密钥（anon/public key）
# 从 Project Settings > API > anon public key 获取
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase JWT Secret
# 从 Project Settings > API > JWT Secret 获取
# 用于验证 JWT 令牌的签名
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ========================================
# 应用配置
# ========================================

# 应用端口（SCF 要求 9000）
PORT=9000

# 环境：development, staging, production
ENVIRONMENT=development

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ========================================
# 认证配置
# ========================================

# 访问令牌过期时间（秒）
# 默认: 3600 (1 小时)
ACCESS_TOKEN_EXPIRY=3600

# 刷新令牌过期时间（秒）
# 默认: 604800 (7 天)
REFRESH_TOKEN_EXPIRY=604800

# 密码最小长度
# 默认: 8
MIN_PASSWORD_LENGTH=8

# ========================================
# 安全配置
# ========================================

# CORS 允许的源（多个用逗号分隔）
# 开发环境: http://localhost:3000,http://localhost:9000
CORS_ORIGINS=http://localhost:3000,http://localhost:9000

# 速率限制（每分钟请求次数）
RATE_LIMIT_PER_MINUTE=60

# ========================================
# 注意事项
# ========================================

# 1. 绝不要将 .env 文件提交到版本控制系统
# 2. 在生产环境中，使用环境变量而不是 .env 文件
# 3. JWT Secret 是敏感信息，妥善保管
# 4. 定期轮换密钥和令牌
