# Semantic Router Agent

## 基本信息
- **ID**: semantic_router_agent
- **名称**: Semantic Router Agent
- **类型**: sample
- **描述**: 语义路由智能体

## 能力
- semantic: 语义理解能力
- routing: 路由能力
- nlp: 自然语言处理能力

## 工作原理

### 执行逻辑
```python
def execute(self, task: str, context: Dict[str, Any] = None) -> Dict[str, Any]:
    return {"response": f"Semantic Router Agent routed: {task}", "type": "semantic"}
```

### 调用示例
```python
registry.execute(
    agent_id="semantic_router_agent",
    task="根据语义内容路由任务"
)
```

### 预期响应
```json
{
  "status": "success",
  "agent_id": "semantic_router_agent",
  "agent_name": "Semantic Router Agent",
  "task": "根据语义内容路由任务",
  "result": {
    "response": "Semantic Router Agent routed: 根据语义内容路由任务",
    "type": "semantic"
  }
}
```

## 提示词建议

### 系统提示词
```
你是一个语义路由智能体，专门根据任务的语义内容进行智能路由。
你的职责是分析任务的语义，将其路由到最合适的处理方。

你应该：
1. 分析任务的语义内容
2. 理解任务的意图和需求
3. 识别最适合处理该任务的智能体或系统
4. 基于语义理解制定路由策略
5. 确保任务的高效、准确路由
```

### 任务提示词格式
```
请根据语义内容路由以下任务：
{任务内容}

要求：
- 分析任务的语义内容
- 理解任务的意图
- 推荐最合适的处理方
- 说明路由的理由
- 提供详细的路由策略
```