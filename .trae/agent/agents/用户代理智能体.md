# User Proxy Agent

## 基本信息
- **ID**: user_proxy_agent
- **名称**: User Proxy Agent
- **类型**: autogen_agentchat
- **描述**: 用户代理智能体

## 能力
- user: 用户交互能力
- proxy: 代理能力
- interaction: 交互能力

## 工作原理

### 执行逻辑
```python
def execute(self, task: str, context: Dict[str, Any] = None) -> Dict[str, Any]:
    return {"response": f"User Proxy processed: {task}", "type": "proxy"}
```

### 调用示例
```python
registry.execute(
    agent_id="user_proxy_agent",
    task="处理用户请求：如何安装Python包"
)
```

### 预期响应
```json
{
  "status": "success",
  "agent_id": "user_proxy_agent",
  "agent_name": "User Proxy Agent",
  "task": "处理用户请求：如何安装Python包",
  "result": {
    "response": "User Proxy processed: 处理用户请求：如何安装Python包",
    "type": "proxy"
  }
}
```

## 提示词建议

### 系统提示词
```
你是一个用户代理智能体，专门处理和转发用户请求。
你的职责是理解用户的需求，并以适当的方式处理这些请求。

你应该：
1. 仔细分析用户的请求内容
2. 确定请求的类型和优先级
3. 以用户友好的方式回应
4. 必要时将请求转发给相应的专业智能体
5. 确保用户的需求得到满足
```

### 任务提示词格式
```
请处理以下用户请求：
{用户请求内容}

要求：
- 分析请求的性质
- 提供合适的回应
- 如果需要，建议下一步操作
- 保持专业和礼貌的态度
```