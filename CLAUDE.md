# CLAUDE.md

## 项目上下文

- 根目录：`c:\Users\Administrator\Desktop\my-text`
- 规则体系：L1(AGENTS.md) → L2(CLAUDE.md) → L3(.trae/rules/*.md)

## 技术栈

- Python 3.12
- Node.js
- PowerShell
- TRAE IDE

## 常用命令

```bash
# 规则相关
ls .trae/rules/          # 查看L3规则
cat CLAUDE.md            # 查看L2规则
cat AGENTS.md            # 查看L1规则

# 项目结构
ls                       # 查看当前目录
tree /F .trae            # 查看.trae目录树
```

## 代码风格

### 命名规范
- 变量：camelCase（userName, isActive）
- 类：PascalCase（UserService, ConfigLoader）
- 常量：UPPER_SNAKE_CASE（MAX_RETRY, API_KEY）
- 文件：kebab-case 或 snake_case（user-service.py, user_service.py）

### 必须遵循
- 缩进：4空格
- 类型提示：所有函数必须标注类型
- 导入顺序：标准库 → 第三方库 → 本地模块

### 禁止清单
- 禁止 var、console.log、硬编码值
- 禁止空 import 或 pass 占位
- 禁止一次性代码（无复用性）

## 工作流程

### 任务执行
1. 先调研（本地沉淀 → 外部搜索）
2. 再执行（优先复用现有工具）
3. 完成后指出1个可优化点

### 决策流程
1. 明确可验证依据
2. 评估多个方案
3. 选择最优解
4. 记录决策理由

### 风险操作
- 删除/修改前必须解释意图并等待确认
- 创建文件夹前必须请示用户

## 项目规范

### 文件组织
- .trae/rules/*.md：L3功能规则
- .trae/memories/：记忆系统（user/project/feedback/reference）
- .trae/mcp/：MCP服务配置
- .trae/skills/：技能模块

### 规则管理
- 以注意力机制为核心
- 避免冗余，优先合并
- L1/L2变更必须触发链路扫描

## 上下文管理指针

### 当前焦点
- 规则体系优化：吸取Claude Code最佳实践

### 最近变更
- 2026-05-19 新增L1.7/L2.7/L3.8动态加载规则

### 已知问题
- MCP前端配置需用户手动创建

---

## 核心行为规则（必加载）

L2.1 回答简洁，不超过4行。

L2.2 风险操作前需确认。

L2.3 先调研，再执行；调研时优先在本机查找沉淀的知识和工具，优先复用而非重复造轮子。

L2.4 产出满足质量标准：准确（信息/代码正确）、完整（无关键遗漏）、清晰（逻辑明确）、可执行（具体可操作）；代码规范遵循L3.1，测试遵循L3.2，安全遵循L3.3；任务完成后主动指出至少1个可优化点。

## 扩展行为规则（按需加载）

L2.5 复杂任务分执行与监督环节，监督基于全新上下文独立评估并给出结论。

L2.6 规则管理/变更以注意力为核心，避免冗余，优先合并。

L2.7 决策需基于可验证依据，避免主观臆断。

L2.9 维护上下文管理指针：当前焦点、最近变更（含时间戳）、已知问题。

L2.10 规则可执行性：所有规则必须包含"条件→动作→结果"结构，确保可操作、可验证。

L2.11 代码文档采用Markdown格式组织，便于后续检索、复用和知识沉淀。

L2.12 提示词设计遵循清晰、具体、可验证原则，提高AI响应质量。

L2.13 技能开发优先复用现有技能库，按需扩展，避免重复造轮子。