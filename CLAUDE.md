# CLAUDE.md

## 项目上下文

- 根目录：`c:\Users\Administrator\Desktop\my-text`
- 规则体系：L1(AGENTS.md) → L2(CLAUDE.md) → L3(.trae/rules/*.md)

## 技术栈
- Python 3.12 / Node.js / PowerShell / TRAE IDE

## 常用命令
```bash
ls .trae/rules/    # 查看L3规则
cat CLAUDE.md       # 查看L2规则
cat AGENTS.md       # 查看L1规则
```

## 代码风格
- 变量：camelCase | 类：PascalCase | 常量：UPPER_SNAKE_CASE
- 缩进：4空格 | 类型提示：必须标注 | 导入顺序：标准库→第三方→本地

## 禁止清单
- 禁止 var、console.log、硬编码值
- 禁止空 import 或 pass 占位
- 禁止一次性代码（无复用性）

---

## 核心行为规则（必加载）

### L2.1 工作流程
先调研（本地沉淀→外部搜索），再执行（优先复用现有工具），完成后指出1个可优化点。

### L2.2 风险操作
删除/修改前必须解释意图并等待确认。创建文件夹前必须请示用户。

### L2.3 质量标准
产出满足质量标准：准确（信息/代码正确）、完整（无关键遗漏）、清晰（逻辑明确）、可执行（具体可操作）。

### L2.4 决策规范
决策需基于可验证依据，避免主观臆断。评估多个方案，选择最优解，记录决策理由。

### L2.5 任务管理
复杂任务分执行与监督环节。监督基于全新上下文独立评估并给出结论。

### L2.6 上下文指针
维护上下文管理指针：当前焦点、最近变更（含时间戳）、已知问题。

### L2.7 规则可执行性
所有规则必须包含"条件→动作→结果"结构，确保可操作、可验证。

### L2.8 记忆管理
使用CLAUDE.md存储项目上下文。规则管理使用AGENTS.md。

---

## 扩展行为规则（按需加载）

### L2.9 编码规范
遵循L3.1编码规范模块。代码必须有适当缩进（4空格）。优先使用现有工具和库。

### L2.10 测试规范
遵循L3.2测试规范模块。修改代码后必须验证正确性。测试失败必须报告原因。

### L2.11 安全规范
遵循L3.3安全规范模块。不生成恶意代码。不泄露敏感信息。

### L2.12 动态加载
遵循L3.8动态加载模块。根据上下文自动切换规则集。维护上下文指针。

### L2.13 反馈优化
遵循L3.9反馈模块。记录规则执行日志。生成优化建议。

### L2.14 规则管理
遵循L3.6规则管理模块。新增L3模块必须经过提案→审核→通过。复杂任务分执行与监督。

### L2.15 提示词工程
掌握计划模式和探索模式。合理控制思考预算。设计清晰、具体、可验证的提示词。

### L2.16 上下文工程
优化文件选择策略。实现上下文压缩和恢复。监控Token使用。

### L2.17 成本优化
采用Token节省策略：缓存、限流、Prompt压缩。

### L2.18 技能开发
优先复用现有技能库，按需扩展，避免重复造轮子。支持子代理模式。

### L2.19 自动化
支持无头模式、定时任务、事件驱动自动化。集成GitHub PR/CI流程。

### L2.20 安全实践
遵循权限系统、沙箱隔离、安全发布和回滚流程。

---

## 项目规范

### 文件组织
- .trae/rules/*.md：L3功能规则（支持最多3层嵌套）
- .trae/memories/：记忆系统
- .trae/skills/：技能模块

### 规则管理
- 规则之间不得彼此冲突
- 以L1层规则为准
- 支持动态加载

## 上下文管理指针
- 当前焦点：规则体系优化
- 最近变更：2026-05-19 精简规则体系
- 已知问题：MCP前端配置需用户手动创建
