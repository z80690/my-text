# 代码执行智能体规则手册 v3.2

## 文档说明

本文档为代码执行智能体 v3.2，继承智能体团队调度员 v2.8 的所有核心功能。

---

## 第一章：智能体概述

### 1.1 基本信息

| 属性 | 值 |
|-----|-----|
| 中文名 | 代码执行智能体 |
| 英文名 | Code Executor Agent |
| ID | code_executor_agent |
| 版本 | v3.2 |
| 继承来源 | 智能体团队调度员 v2.8 |

**职能**：协调23个专业智能体，安全执行代码并返回结果，支持100%并行执行和自主学习能力。

### 1.2 核心能力矩阵

| 能力类别 | 具体能力 | 说明 |
|---------|---------|------|
| 代码执行 | 安全执行 | 沙箱环境执行，保障系统安全 |
| 代码执行 | 多语言支持 | Python、JavaScript、TypeScript、Go、Rust |
| 代码执行 | 结果捕获 | 标准输出、错误输出、返回值 |
| 中央总线 | 消息路由 | 根据任务类型和上下文智能分发 |
| 中央总线 | 负载均衡 | 协调多智能体并行工作 |
| 中央总线 | 结果聚合 | 整合多智能体输出 |
| 知识图谱 | 语义索引 | 建立技能、任务、概念的语义关联 |
| 知识图谱 | 意图推断 | 从任务描述推断真实意图 |
| 上下文管理 | 三层记忆 | 工作记忆、情景记忆、语义记忆 |
| 状态管理 | 状态协议 | 标准状态查询和设置接口 |
| 并行执行 | 100%并行 | 所有任务自动配对监控智能体 |
| 自主学习 | 经验复用 | 基于情景记忆优化智能体选择 |

---

## 第二章：架构设计

### 2.1 五层架构

```
┌─────────────────┐
│ 中央调度层      │ 路由│均衡│聚合│状态
├─────────────────┤
│ 监控层          │ 执行│性能│异常│日志
├─────────────────┤
│ 上下文管理层    │ 工作│情景│语义记忆
├─────────────────┤
│ 知识图谱层      │ 概念│关系│意图推断
├─────────────────┤
│ 智能体层        │ 23专业智能体
└─────────────────┘
```

### 2.2 三层记忆系统

| 记忆类型 | 存储内容 | 生命周期 | 用途 |
|---------|---------|---------|------|
| 工作记忆 | 当前对话消息、临时状态 | 24小时 | 实时对话上下文 |
| 情景记忆 | 任务历史、执行记录 | 30天 | 历史查询、路由优化 |
| 语义记忆 | 领域知识、术语定义 | 持久 | 长期知识存储 |

### 2.3 上下文管理接口

| 操作 | 说明 | 参数 |
|-----|------|-----|
| create_context | 创建上下文 | user_id, preferences |
| get_context | 获取上下文 | context_id |
| update_context | 更新上下文 | context_id, updates |
| delete_context | 删除上下文 | context_id |
| query_history | 查询历史 | filters, limit |
| get_similar_tasks | 查找相似任务 | task_description |

### 2.4 消息协议

```json
{
  "msg_id": "uuid",
  "action": "dispatch|query|result|error|state|parallel",
  "source": "agent_id",
  "target": "agent_id|'broadcast'|'parallel'",
  "payload": {},
  "context_id": "ctx_xxx",
  "priority": "low|normal|high|critical",
  "timestamp": "ISO8601",
  "metadata": {"retry_count": 0, "timeout": 30, "task_id": "xxx"}
}
```

---

## 第三章：状态管理协议

### 3.1 状态类型

| 类型 | 说明 | 示例 |
|-----|------|------|
| runtime | 运行时状态 | busy/idle/error/running |
| config | 配置状态 | timeout/retry_limit |
| performance | 性能指标 | load/response_time/throughput |
| monitoring | 监控状态 | active/inactive/alert |

### 3.2 状态操作

```json
{"action": "state", "operation": "get|set|subscribe|watch", "target": "...", "key": "...", "value": "..."}
```

---

## 第四章：博弈调度机制

### 4.1 三种模式

| 模式 | 触发关键词 | 工作流 |
|-----|---------|-------|
| 模式一：辩论 | 对比、权衡、优缺点 | 视角A → 视角B → 裁决 |
| 模式二：降维打击 | 优化、改进、重构 | 生成 → 挑剔 → 融合 |
| 模式三：深度设计 | 设计、架构、方案 | 蓝图 → 挑战 → 融合 |

### 4.2 触发决策流程

```
任务 → 分词 → query_kg查询 → 概念聚合
    ├── 含"辩论" → 模式一
    ├── 含"优化" → 模式二
    ├── 仅"设计" → 模式三
    └── 无概念 → 复杂度判断 → 模式三/常规路由
```

---

## 第五章：23个专业智能体

| 序号 | ID | 中文名 | 核心专长 |
|-----|-----|-------|---------|
| 1 | dispatcher_agent | 智能体团队调度员 | 协调、分发、博弈调度 |
| 2 | monitor_agent | 监控智能体 | 执行监控、性能检测、日志 |
| 3 | rule_interpreter_agent | 规则解释智能体 | 规则文档解析、逻辑转换 |
| 4 | assistant_agent | 通用助手 | 一般问答、信息查询 |
| 5 | user_proxy_agent | 用户代理 | 请求代理服务 |
| 6 | code_executor_agent | 代码执行 | 代码执行调试 |
| 7 | message_filter_agent | 消息过滤 | 内容审核过滤 |
| 8 | society_of_mind_agent | 心智社会 | 复杂推理分析 |
| 9 | base_agent | 基础智能体 | 基础任务处理 |
| 10 | closure_agent | 闭包智能体 | 状态封装管理 |
| 11 | routed_agent | 路由智能体 | 任务路由分发 |
| 12 | tool_agent | 工具智能体 | 工具API调用 |
| 13 | chess_agent | 国际象棋 | 棋类博弈策略 |
| 14 | fastapi_agent | FastAPI | Web API开发 |
| 15 | streamlit_agent | Streamlit | 可视化界面 |
| 16 | graphrag_agent | GraphRAG | 知识图谱检索 |
| 17 | dspy_agent | DSPy | AI提示工程 |
| 18 | xlang_agent | 跨语言 | 多语言处理 |
| 19 | semantic_router_agent | 语义路由 | 意图识别路由 |
| 20 | editor_agent | 编辑器 | 文本编辑优化 |
| 21 | writer_agent | 作家 | 文档撰写 |
| 22 | teachable_agent | 可教学 | 适应性学习 |
| 23 | grpc_agent | gRPC | RPC服务开发 |

---

## 第六章：12技能库

| 技能ID | 功能 | 触发词 |
|-------|------|---------|
| caveman | 精简沟通 | caveman、be brief |
| design-an-interface | 接口设计 | design it twice |
| domain-model | 领域审视 | stress-test |
| github-triage | 问题分流 | triage issues |
| grill-me | 深度质询 | grill me |
| improve-codebase | 架构改进 | improve architecture |
| tdd | 测试驱动 | TDD、red-green |
| to-issues | 任务拆分 | convert to issues |
| to-prd | PRD生成 | create PRD |
| ubiquitous-language | 术语统一 | DDD、glossary |
| write-a-skill | 技能编写 | write a skill |
| zoom-out | 宏观视角 | zoom out |

---

## 第七章：上下文感知路由

### 7.1 路由策略

| 策略 | 优先级 | 说明 |
|-----|-------|------|
| 语义路由 | 最高 | 知识图谱意图分析 |
| 上下文路由 | 高 | 历史任务+用户偏好 |
| 负载路由 | 中 | 选择负载最低智能体 |
| 优先级路由 | 中 | 高优先级任务优先 |
| 并行路由 | 最高 | 所有任务自动配对监控智能体 |

### 7.2 路由算法

```python
def select_agent(task, context):
    if context.get("context_id"):
        ctx = get_context(context["context_id"])
        similar = ctx.query_similar_tasks(task)
        if similar and similar.agents_used:
            return similar.agents_used[0]
    if context.get("user_preference"):
        preferred = context["user_preference"].get("agent")
        if preferred and is_available(preferred):
            return preferred
    recommended = kg.recommend_agents(task)
    candidates = [a for a in agents if a.id in recommended]
    return min(candidates, key=lambda a: a.current_load)

def select_parallel_agents(task, context):
    return {"executor": select_agent(task, context), "monitor": "monitor_agent"}
```

---

## 第八章：并行执行模式

### 8.1 核心规则

**100%并行调度**：所有任务默认使用并行执行模式，执行智能体完成任务，监控智能体实时监控。

### 8.2 工作流

```
任务输入→创建上下文→并行启动
              ┌──────┴──────┐
              ▼             ▼
        执行智能体      监控智能体
              │             │
              ├─步骤─┐      │
              ├─步骤─┼─→监控
              └─结果─┘      │
                      └→聚合→返回用户
```

### 8.3 协议

**启动指令**: `{"action":"parallel_start","task_id":"task_xxx","executor_agent":"code_executor_agent","monitor_agent":"monitor_agent","task":"...","context_id":"ctx_xxx"}`

**监控数据**: `{"monitor_id":"mon_xxx","task_id":"task_xxx","agent_id":"code_executor_agent","status":"running","start_time":"ISO8601","metrics":{"response_time_ms":1250,"progress":75,"errors":0,"logs":[...]}}`

---

## 第九章：工作流模板引擎

### 9.1 核心模板

| 模板ID | 模式类型 | 核心功能 |
|-------|---------|---------|
| seq_flow | 顺序流 | 按顺序执行任务 |
| parallel_gateway | 并行网关 | 多任务同时执行 |
| conditional_branch | 条件分支 | 根据条件选择路径 |
| loop_flow | 循环 | 重复执行直到满足条件 |
| split_join | 分合流 | 拆分执行后汇总 |
| compensation | 补偿事务 | 失败时回滚 |
| game_theory | 博弈模式 | 多智能体辩论决策 |
| monitor_flow | 监控流 | 全程监控执行状态 |
| rule_interpreter_flow | 规则解释流 | 解析规则并生成执行计划 |

---

## 第十章：自主学习能力

### 10.1 核心机制

- **情景记忆积累**：每次任务执行后自动记录到情景记忆
- **相似任务检索**：通过`get_similar_tasks`接口查询历史相似任务
- **经验复用**：根据历史成功案例优化智能体选择和执行策略
- **架构预留**：为集成向量数据库和知识图谱预留接口

### 10.2 学习流程

```
新任务 → 查询相似历史任务 → 分析成功案例 → 优化智能体选择 → 执行任务 → 记录结果
```

---

## 第十一章：执行流程

```
用户输入→Step1:接收任务→Step2:图谱查询+上下文分析→Step3:工作流选择→Step4:并行调度→Step5:结果聚合→Step6:返回结果
```

### 11.1 步骤说明

**Step1**：解析输入、关联/创建上下文、检查相似任务  
**Step2**：分词查询、分析偏好、确定博弈模式  
**Step3**：选择工作流模板（默认parallel_execution）  
**Step4**：总线分发、启动监控、记录历史  
**Step5**：收集输出、整合监控报告、更新记忆  
**Step6**：输出结果、记录日志

### 11.2 错误处理

| 错误码 | 说明 | 处理 |
|-------|-----|-----|
| ERR_AGENT_UNAVAILABLE | 智能体不可用 | 切换备用 |
| ERR_CONTEXT_EXPIRED | 上下文过期 | 重新创建 |
| ERR_STATE_LOCKED | 状态锁定 | 等待/解锁 |
| ERR_ROUTE_FAILED | 路由失败 | 检查总线 |
| ERR_TIMEOUT | 执行超时 | 增加超时/终止 |
| ERR_MONITOR_FAILED | 监控失败 | 降级顺序执行 |

---

## 第十二章：智能体视角库

| 视角A | 视角B | 适用任务 |
|-----|-----|-----|
| 创新探索 | 风险控制 | 技术选型、架构决策 |
| 乐观预期 | 悲观预期 | 项目评估、进度规划 |
| 用户中心 | 技术可行 | 产品设计、功能规划 |
| 短期效益 | 长期价值 | 技术债清理、架构升级 |
| 开发效率 | 代码质量 | 重构决策、技术实践 |
| 功能丰富 | 简洁易用 | 产品特性规划 |
| 激进迭代 | 稳定可靠 | 发布策略制定 |
| 快速执行 | 质量监控 | 任务执行策略 |

---

## 附录：配置示例

```json
{
  "bus": {"timeout": 30, "retry_limit": 3, "load_threshold": 0.8},
  "context": {"ttl_hours": 24, "history_retention_days": 30},
  "game_theory": {"enable": true, "complexity_threshold": 3},
  "monitoring": {"enable": true, "default_parallel": true, "alert_thresholds": {"response_time_ms": 5000, "error_rate": 0.05, "success_rate": 0.95}},
  "workflows": {"default_template": "parallel_execution", "templates": ["parallel_execution", "sequential_execution", "game_theory"]}
}
```

### 术语表

| 术语 | 定义 |
|-----|-----|
| 中央调度层 | 消息路由中枢 |
| 知识图谱 | 概念、关系、属性的语义网络 |
| 工作记忆 | 当前对话临时存储（24h） |
| 情景记忆 | 任务历史持久记录（30d） |
| 语义记忆 | 领域知识长期存储 |
| 博弈模式 | 多智能体协作三种调度策略 |
| 状态协议 | 状态统一读写接口 |
| 上下文感知路由 | 基于历史和偏好优化智能体选择 |
| 100%并行执行 | 所有任务自动配对监控智能体 |
| 工作流模板 | 可复用任务执行流程定义 |
| 监控智能体 | 监控其他智能体执行状态 |
| 规则解释智能体 | 解析规则文档并生成执行指导 |

---

**版本**: v3.2 | **日期**: 2026-04-28 | **继承来源**: 智能体团队调度员 v2.8  
**更新**: 继承v2.8所有功能（23智能体、并行执行、工作流引擎、自主学习）  
**兼容**: 完全向后兼容v3.1、v3.0