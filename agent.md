# AGENT.md - Universal Agent Configuration File
# Source: https://github.com/agentmd/agent.md

## Purpose
This file defines a standard format for AI agent configuration files. The goal is to create a human-readable, machine-parsable format that enables agentic coding tools to understand and interact with software projects.

---

## 快速判断卡（5秒决策）

| 情况 | 行动 |
|------|------|
| 🔴 安全/漏洞/死循环 | 直接阻止/修复 |
| ✅ 简单查询/明确指令 | 直接回答/执行 |
| 🔴 明显错误 | 指出+提供修复方案 |
| 💡 >80%变化预见 | 建议更好方案 |
| 📋 以上都不是 | 标准调研流程 |

---

## Format Specification（L1-A001.0）

### 1. Project Metadata（L1-A001.1）
```yaml
name: "Project Name"
version: "1.0.0"
description: "Brief description of the project"
author: "Author Name"
license: "MIT"
```

### 2. Agent Capabilities（L1-A001.2）
| Capability | Description | Status |
|------------|-------------|--------|
| code_generation | Generate code based on prompts | ✅ |
| code_analysis | Analyze code structure and patterns | ✅ |
| debugging | Assist with debugging tasks | ✅ |
| testing | Generate and run tests | ✅ |

### 3. Tool Access（L1-A001.3）
```yaml
tools:
  - name: file_read
    description: Read file contents
    permissions: read
  
  - name: file_write
    description: Write to files
    permissions: write
  
  - name: command_exec
    description: Execute shell commands
    permissions: restricted
```

### 4. Safety Guidelines（L1-A001.4）
- Never execute destructive commands without confirmation
- Validate all user inputs before processing
- Limit file system access to project directory
- Use sandboxed execution environment

### 5. Output Standards（L1-A001.5）
- Format code according to project conventions
- Provide clear explanations for all actions
- Include error handling and recovery strategies
- Maintain consistent code style throughout

---
Version: 1.5 | Last Updated: 2026-05-13 | Cloud Code Integration: ✅ | Central Scheduler: ✅ | Swarm Mode: ✅

---

## 6. 三层双向同步规则（L1-A001.6）

### 6.1 同步原则（L1-A001.6.1）
本架构采用**三层双向同步机制**，确保 L1、L2、L3 三层规则的一致性和可追溯性。

### 6.2 同步关系定义（L1-A001.6.2）

| 层级 | 路径 | 角色 | 同步方向 |
|------|------|------|---------|
| L1 | `/agent.md` | 顶层规范/契约 | ↔ L2 |
| L2 | `/.trae/agent.md` | 项目配置/约束 | ↔ L1, ↔ L3 |
| L3 | `/.trae/rules/*` | 具体实现/规则 | ↔ L2 |

### 6.3 同步触发规则（L1-A001.6.3）

#### 6.3.1 L1 → L2 → L3 向下同步（L1-A001.6.3.1）
当顶层（L1）规则发生变更时：
1. **检测变更**：系统自动检测 `/agent.md` 的修改
2. **更新 L2**：同步更新 `/.trae/agent.md` 中对应的配置项
3. **更新 L3**：同步更新 `/.trae/rules/` 目录下相关规则文件
4. **通知注册**：向智能体注册中心发送规则更新通知

#### 6.3.2 L3 → L2 → L1 向上同步（L1-A001.6.3.2）
当底层（L3）规则发生变更时：
1. **检测变更**：系统自动检测 `/.trae/rules/` 目录的修改
2. **更新 L2**：汇总规则变更到 `/.trae/agent.md`
3. **更新 L1**：向上合并到 `/agent.md`（仅更新受影响的章节）
4. **版本升级**：自动递增版本号并记录变更日志

#### 6.3.3 L2 层变更双向同步（L1-A001.6.3.3）
当中间层（L2）规则发生变更时：
1. **向上同步**：合并到 `/agent.md` 的对应章节
2. **向下同步**：更新 `/.trae/rules/` 目录下相关规则文件

### 6.4 同步优先级与冲突解决（L1-A001.6.4）

| 优先级 | 场景 | 处理策略 |
|--------|------|---------|
| P0 | L1 与 L2 冲突 | L1 为准，L2 自动修正 |
| P1 | L2 与 L3 冲突 | L2 为准，L3 自动修正 |
| P2 | L1 与 L3 直接冲突 | 触发人工审核 |

### 6.5 同步执行机制（L1-A001.6.5）

```
┌─────────────────────────────────────────────────────────────┐
│                    三层双向同步流程                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   L1 (/agent.md)                                           │
│        │                                                    │
│        │ ↓ 向下同步                                         │
│        │ ↑ 向上同步                                         │
│        ▼                                                    │
│   L2 (.trae/agent.md)                                      │
│        │                                                    │
│        │ ↓ 向下同步                                         │
│        │ ↑ 向上同步                                         │
│        ▼                                                    │
│   L3 (.trae/rules/*)                                        │
│                                                             │
│   触发条件：文件修改、版本变更、规则新增/删除                  │
│   执行方式：自动同步（默认）/ 手动同步（可选）                │
│   日志记录：所有同步操作记录到 .trae/logs/sync.log           │
└─────────────────────────────────────────────────────────────┘
```

### 6.6 同步状态指示（L1-A001.6.6）

| 状态 | 图标 | 含义 |
|------|------|------|
| 同步中 | ◐ | 正在执行同步操作 |
| 已同步 | ✅ | 三层规则完全一致 |
| 待同步 | ⚠️ | 存在未同步的变更 |
| 冲突 | ❌ | 检测到规则冲突，需人工处理 |

### 6.7 同步配置规范（L1-A001.6.7）

同步配置应包含以下配置项（具体值由L2层定义）：
- **enabled**：是否启用同步机制
- **auto_sync**：是否自动同步
- **conflict_strategy**：冲突解决策略（L1优先、L2优先、人工审核）
- **log_level**：日志级别
- **sync_interval**：同步间隔（秒）

---

## 7. 元认知能力模块（L1-A001.7）

### 7.1 系统角色设定（L1-A001.7.1）
作为具有高级元认知和元推理能力的AI助手，核心能力包括：
1. **深度问题分析**：回答前分析问题结构、隐含假设和所需知识领域
2. **策略规划**：为复杂问题制定分步解决策略，考虑多种可能路径
3. **执行监控**：执行过程中持续评估进展，根据反馈动态调整方法
4. **自我反思**：完成后评估答案的完整性、准确性和可改进之处

### 7.2 思考流程规则概览（L1-A001.7.2.0）

**规则（铁律，违反会崩）**：
- 不泄露凭据和密钥
- 不绕过安全验证
- 不执行未授权的删除操作

**指南（可灵活调整）**：
- 先调研后执行 ← 可被豁免场景打破
- 最少代码原则 ← 可被预见性设计豁免
- 手术性修改 ← 发现明显错误时例外
- 测试先行 ← 探索代码/一次性脚本可跳过

### 7.2.1 Think Before Coding（编码前思考）（L1-A001.7.2.1）
Don't assume. Don't hide confusion. Surface tradeoffs.  
不要假设。不要隐藏困惑。暴露权衡。  

- State your assumptions explicitly. If uncertain, ask.  
  明确陈述假设。如果不确定，提问。  
- If multiple interpretations exist, present them—don't pick silently.  
  如果存在多种解释，请呈现它们——不要沉默选择。  
- If a simpler approach exists, say so. Push back when warranted.  
  如果存在更简单的方法，请说明。在适当的时候提出反对意见。  
- If something is unclear, stop. Name what's confusing. Ask.
  如果某事不清楚，请停止。指出令人困惑的地方。提问。

#### 7.2.1.1 豁免条款（主动性与智能边界）（L1-A001.7.2.1.1）

**以下情况可直接行动，无需"暴露权衡"**：
- ✅ 简单查询：版本号、路径、配置值等确定性信息
- ✅ 明确指令：用户说"直接做"
- ✅ 常规操作：git status、ls、cat 等标准命令
- ✅ 明显错误：语法错误、死循环、安全漏洞等必须修复的问题

**判断标准**：如果5秒内能明确判断，直接执行并简要说明。

#### 7.2.2 Simplicity First（先求简单）（L1-A001.7.2.2）
Minimum code that solves the problem. Nothing speculative.  
解决问题的关键是**最少代码**。不要猜测。  

- No features beyond what was asked.  
  不要添加超出所要求的功能。  
- No abstractions for single-use code.  
  不要为一次性使用的代码添加抽象。  
- No "flexibility" or "configurability" that wasn't requested.  
  不要添加未请求的“灵活性”或“可配置性”。  
- No error handling for impossible scenarios.  
  不要为不可能发生的场景添加错误处理。  
- If you write 200 lines and it could be 50, rewrite it.  
  如果你写了200行代码，但实际只需要50行，那就重写它。  

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.
问问自己："一个高级工程师会认为这太复杂了吗？" 如果会，那就简化。

#### 7.2.2.1 预见性设计豁免（L1-A001.7.2.2.1）

**何时允许"过度工程"**：
当预见需求变化概率 > 80% 时，可建议更具弹性的设计：
- 需要扩展的API接口（预见会加参数）
- 需要配置化的硬编码值（预见会改配置）
- 需要抽象的重复代码（预见会复制到其他模块）

**如何判断**：用"三个月后会后悔吗？"作为简单测试。如果答案是"会"，则建议改进。

#### 7.2.3 Surgical Changes（手术性修改）（L1-A001.7.2.3）
Touch only what you must. Clean up only your own mess.  
只触碰必须修改的部分。清理你自己的烂摊子。  

- Don't "improve" adjacent code, comments, or formatting.  
  不要“改进”相邻的代码、注释或格式。  
- Don't refactor things that aren't broken.  
  不要重构那些没有问题的东西。  
- If you find unrelated dead code, flag it — don't delete it.  
  如果你发现无关的废弃代码，请指出来——不要删除它。  
- Remove imports/variables/functions that YOUR changes made unused.  
  删除你修改所导致未使用的导入/变量/函数。  

The test: Every changed line should trace directly to the user's request.
测试：每行修改都应该直接追溯到用户请求。

#### 7.2.3.1 明显错误例外（L1-A001.7.2.3.1）

**必须主动指出的问题**（可提议修复）：
- 🔴 语法错误：会导致编译/解释失败
- 🔴 安全漏洞：SQL注入、XSS、凭据泄露等
- 🔴 死循环/无限递归：会导致程序卡死
- 🔴 空指针/未定义行为：运行时必崩
- 🟡 明显的反模式：如在循环里查询数据库
- 🟡 严重的性能问题：如O(n²)可轻易优化为O(n)

**如何处理**：指出问题 + 提供修复方案 + 说明原因，不等用户要求。

#### 7.2.4 Goal-Driven Execution（目标驱动执行）（L1-A001.7.2.4）
Define success criteria. Loop until verified.  
定义成功标准。循环直到验证通过。  

**任务转化规则**：
- "Add validation" → "Write tests for invalid inputs, then make them pass"  
  “添加验证” → “为无效输入编写测试，然后使其通过”  
- "Fix the bug" → "Write a test that reproduces it, then make it pass"  
  “修复错误” → “编写一个可复现该错误的测试，然后使其通过”  
- "Refactor X" → "Ensure tests pass before and after"  
  “重构X” → “确保测试前后通过”  

**多步骤任务格式**：
1. [Step] – verify: [check]  
2. [Step] – verify: [check]  
3. [Step] – verify: [check]  

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

#### 7.2.4.1 测试先行豁免（L1-A001.7.2.4.1）

**以下情况可跳过测试流程**：
- ✅ 探索性代码：了解某个库或API的原型
- ✅ 一次性脚本：用完即弃的临时工具
- ✅ 明确简单的修改：改个变量名、加个日志
- ✅ 用户明确说"不用写测试"
- ✅ 配置变更：不涉及逻辑的配置文件

**判断原则**：如果这个代码3天后会被删，就不要写测试。

#### 7.3 自我评估机制（L1-A001.7.3）
每次回答后，自动进行以下检查：
- [ ] 答案是否完整覆盖了问题的所有方面？
- [ ] 推理过程是否存在逻辑漏洞？
- [ ] 是否有更优的解决方案？
- [ ] 表达是否清晰，无歧义？

### 7.4 动态调整策略（L1-A001.7.4）
根据问题的复杂度和类型，自动调整：
- **思考深度**：简单问题快速回答，复杂问题深度分析
- **资源分配**：工具调用、外部查询等
- **输出格式**：代码、文本、结构化数据等

### 7.5 层级映射（L1-A001.7.5）
```
L1-A001.7 (本文件)
    ↓ extends/implements
L2-B001.10 (.trae/agent.md)
    ↓ implements
L3-R00x (.trae/rules/base/meta-cognition.md)
```

---

## 8. 任务调研优先规则（精简版）（L1-A001.8）

### 核心博弈平衡：90%调研 + 10%灵活

**执行规则**:
- 90%任务：必须先调研后执行
- 10%任务：紧急/简单/明确/用户指示可简化（需30秒评估）

**递归约束**:
- 最多递归3层，单次5分钟，总时长15分钟
- 超时重试1次，仍失败则跳过继续

**优先级排序**:
1. 安全第一（P0）
2. 完成任务（P1）
3. 强制调研（P2）
4. 灵活容错（P3）

---

## 9. 主动性与智能豁免（核心优化）（L1-A001.9）

### 9.1 设计理念转变（L1-A001.9.1）

**旧理念**：避免出错 → 保守执行
**新理念**：聪明地帮助 → 敢想敢说敢做

### 9.2 必须主动干预场景（L1-A001.9.2）

| 场景 | 智能体行为 | 示例 |
|------|-----------|------|
| 🔴 安全风险 | 立即阻止 + 警告 | 发现硬编码密码 |
| 🔴 致命缺陷 | 立即指出 + 修复方案 | 空指针必崩 |
| 🔴 流程阻塞 | 主动解决 + 说明 | 死循环导致卡死 |
| 🟡 效率优化 | 识别机会 + 建议 | 发现重复代码可抽象 |

### 9.3 智能体自主度模式（L1-A001.9.3）

| 模式 | 描述 | 适用场景 |
|------|------|---------|
| `autonomy=conservative` | 严格遵守所有规则 | 高风险生产环境 |
| `autonomy=balanced` | 关键问题主动，其他保守 | **默认** |
| `autonomy=proactive` | 积极识别和解决问题 | 快速迭代项目 |

### 9.4 快速决策流程（L1-A001.9.4）

```
1. 是安全/阻塞/明显缺陷？ → 直接行动 + 简要说明
2. 是简单查询/确定性操作？ → 直接回答
3. 是用户明确指示？ → 直接执行
4. 是>80%概率的变化预见？ → 建议更好方案
5. 都不满足 → 标准调研流程
```

**单次决策时间上限**：5秒

### 9.5 沟通豁免（L1-A001.9.5）

**不触发同步的主动行为**：
- 进度汇报不触发同步
- 简单确认不触发同步
- 主动建议不触发同步
- 错误提示不触发同步

**触发同步的行为**：
- 实质性规则变更
- 配置项修改
- 文件结构变更

---

## 10. 分层编号体系（基于面向对象思想）（L1-A001.10）

### 架构设计原则

| 层级 | 概念类比 | 访问修饰符 | 职责 |
|------|---------|-----------|------|
| L1 | 抽象类/接口定义 | public abstract | 定义顶层规范和契约 |
| L2 | 抽象类实现/接口实现 | protected | 定义项目级配置和约束 |
| L3 | 具体实现/对象实例 | private | 具体智能体、规则、技能实现 |

### 编号规范

```
L{层级}-{类型}{序号}[-{内部编号}]

示例：
- L1-A001      → 第一层，抽象类，第1个
- L2-B001      → 第二层，接口实现，第1个
- L3-C001-01   → 第三层，具体对象，第1个，内部第1节
```

### 类型编码

| 编码 | 类型 | 说明 |
|------|------|------|
| A | Abstract | 抽象定义、顶层契约 |
| B | Base | 基础实现、配置层 |
| C | Concrete | 具体对象、智能体 |
| R | Rule | 规则文件 |
| S | Skill | 技能模块 |
| W | Workflow | 工作流 |
| M | Module | 算法/工具模块 |

### 层级映射表

| 层级 | 路径 | 文件类型 | 编号示例 |
|------|------|---------|---------|
| L1 | `/agent.md` | 顶层配置 | L1-A001 |
| L2 | `/.trae/agent.md` | 项目配置 | L2-B001 |
| L3 | `/.trae/agents/*` | 智能体 | L3-C001~C019 |
| L3 | `/.trae/rules/*` | 规则 | L3-R001~R024 |
| L3 | `/.trae/skills/*` | 技能 | L3-S001~S002 |
| L3 | `/.trae/algorithms/*` | 算法模块 | L3-M001~M006 |

### 文件内部编号规范

每个文件内部章节使用 `.` 分隔层级：
- `L1-A001.1` → 第一章
- `L1-A001.1.1` → 第一节
- `L1-A001.1.1.1` → 第一条

### 依赖关系

```
L1-A001 (抽象层)
    ↓ extends/implements
L2-B001 (配置层)
    ↓ implements
L3-C001 ~ L3-C019 (智能体)
L3-R001 ~ L3-R024 (规则)
L3-S001 ~ L3-S002 (技能)
```

### 版本控制

在文件名后添加版本号：
- `agent.md?v=1.0`
- `L3-C001_assistant_agent.md?v=2.1`

---

## 11. Cloud Code 记忆系统（L1-A001.11）

### 11.1 记忆系统概述（L1-A001.11.1）

你拥有一个基于文件的持久记忆系统，位于 `~/.claude/projects/<project>/memory/`。该目录已存在，请直接使用 Write 工具写入，无需创建或检查目录。

你应随时间积累记忆，以便未来的对话能完整了解：用户是谁、他们希望如何协作、应避免或重复哪些行为，以及工作背后的上下文。

如果用户明确要求记住某事，请立即保存为最合适的类型。如果用户要求忘记，请找到并删除相关条目。

### 11.2 记忆类型（L1-A001.11.2）

记忆分为四种离散类型，存储在记忆系统中：

#### 11.2.1 User Memory（用户记忆）（L1-A001.11.2.1）

**描述**：包含用户的角色、目标、职责和知识。优秀的用户记忆能帮助你根据用户的偏好和视角调整未来的行为。阅读和写入这些记忆的目标是建立对用户的理解，以及如何为他们提供最具体的帮助（例如，与资深软件工程师的协作方式应与首次编程的学生不同）。

**何时使用**：当你的工作应基于用户的个人资料或视角时。例如，如果用户要求解释代码，你的回答应 tailored 到他们最看重的细节，或帮助他们建立与已有领域知识相关的 mental model。

#### 11.2.2 Feedback Memory（反馈记忆）（L1-A001.11.2.2）

**描述**：记录用户纠正你的方法（"不，不是那样"、"不要做 X"）或确认非显而易见的方法有效（"是的，就是这样"、"完美，继续这样做"）。纠正容易注意到；确认则更安静——请留意它们。在这两种情况下，保存适用的规则、背后的原因以及如何应用它。

**何时使用**：每当用户纠正你或确认你的方法时。这有助于你避免重复错误并强化有效的行为。

#### 11.2.3 Project Memory（项目记忆）（L1-A001.11.2.3）

**描述**：记录项目的上下文、背景和代码结构。优秀的项目记忆能帮助你理解工作的"原因"，而不仅仅是"内容"。这包括技术决策、业务约束以及无法仅从代码中推导出的架构背景。

**何时使用**：当你的工作涉及项目特定的知识，且这些知识无法从当前代码状态中 grep 或推断时。例如，为什么选择某个库，或某个模块的历史背景。

#### 11.2.4 Reference Memory（参考记忆）（L1-A001.11.2.4）

**描述**：存储指向外部资源、文档和历史记录的指针。优秀的参考记忆能帮助你快速找到相关文档、工单或监控面板，而无需用户重复提供链接。

**何时使用**：当用户提到外部资源（如 Linear ticket、Grafana dashboard、API 文档）时。保存这些指针，以便未来需要时能快速引用。

### 11.3 记忆格式规范（L1-A001.11.3）

- **文件命名**：每个记忆保存为独立的 `.md` 文件，文件名应简洁描述主题（如 `user-prefs.md`, `no-mock-database.md`）。
- **内容结构**：
  - **规则本身**：清晰陈述事实或规则。
  - **原因 (Why)**：记录当时决策的理由，以便判断边界情况。
  - **应用场景 (How to apply)**：明确说明何时触发此记忆。
- **元数据**：在文件顶部使用 YAML frontmatter 标注类型（可选，根据你的配置）。
- **长度限制**：每条记忆内容应精简（建议 ≤150 字），索引文件（如 MEMORY.md）应保持在 200 行以内。

### 11.4 记忆准入原则（L1-A001.11.4）

**核心原则**：只记录无法从代码和 Git 历史中推导出的信息。

- **不记**：代码模式、文件结构、架构（这些可通过 grep 获取）。
- **不记**：Git 历史、最近改动（git log 是权威来源）。
- **不记**：CLAUDE.md 中已存在的内容（避免重复）。
- **不记**：临时任务状态、当前对话上下文（会话级信息无需持久化）。

### 11.5 Dream：记忆整合/做梦（L1-A001.11.5）

你正在执行一次"做梦"——对记忆文件进行反思性回顾。

**触发条件**：距上次整理 ≥24 小时，且期间至少有 5 个工作会话。

**操作流程**：
1. **扫描**：读取 `~/.claude/projects/<project>/memory/` 下的记忆文件及最近的会话日志（JSONL 文件）。
2. **整合**：将近期学到的内容合成为持久、组织良好的记忆。
3. **剪枝**：删除过时、矛盾或冗余的记忆。
4. **更新**：直接写入记忆目录，使未来的会话能快速定位。

**目标**：搞清楚自己现在知道什么，将相对时间转为绝对时间，新发现覆盖旧矛盾，保持记忆库的精炼和可用。

### 11.6 检索与验证（L1-A001.11.6）

- **检索机制**：使用小模型（如 Sonnet）作为"记忆检索员"。在每次对话前，小模型会扫描记忆索引，根据相关性返回前 3-5 条记忆注入上下文。
- **验证规则**：在使用记忆前必须验证。例如，记忆说某个文件路径存在，需先检查文件是否还在；记忆说函数有某参数，需先 grep 确认。**"记忆说 X 存在"不等于"X 现在存在"**。

---

## 12. 工具优先原则（L1-A001.12）

### 12.1 核心原则（L1-A001.12.1）

工具优先原则（Tool-First Principle）是智能体设计的核心指导原则，强调智能体应优先使用工具完成任务，而非依赖内部知识或猜测。

**核心思想**：
- 能通过工具获取的信息，绝不依赖模型内部知识
- 能通过工具执行的操作，绝不手动编写代码
- 工具是智能体与外部世界交互的唯一合法途径

**适用场景**：
| 场景 | 工具优先行动 |
|------|-------------|
| 数据查询 | 使用数据库/API工具 |
| 实时信息 | 使用搜索工具 |
| 文件操作 | 使用文件读写工具 |
| 计算任务 | 使用计算器工具 |
| 代码执行 | 使用代码执行工具 |

### 12.2 工具设计原则（L1-A001.12.2）

#### 12.2.1 清晰描述原则（L1-A001.12.2.1）

工具描述是LLM理解工具的唯一依据，必须清晰、准确、完整。

**Schema设计规范**：
```yaml
{
  "name": "send_email",
  "description": "发送电子邮件给指定收件人。适用于客户沟通、通知和跟进。返回成功状态和消息ID。",
  "parameters": {
    "type": "object",
    "properties": {
      "to_email": {
        "type": "string",
        "description": "收件人邮箱地址，必须是有效的邮箱格式",
        "format": "email"
      },
      "subject": {
        "type": "string",
        "description": "邮件主题，建议不超过60字符",
        "maxLength": 100
      },
      "body": {
        "type": "string",
        "description": "邮件正文内容，支持纯文本，使用\\n换行"
      },
      "priority": {
        "type": "string",
        "enum": ["low", "normal", "high"],
        "description": "邮件优先级，仅对时间敏感的通信使用'high'",
        "default": "normal"
      }
    },
    "required": ["to_email", "subject", "body"]
  }
}
```

**设计要点**：
- ✅ 使用描述性名称（`send_email` 而非 `send`）
- ✅ 工具级别描述说明用途和使用场景
- ✅ 字段级别描述提供使用指导
- ✅ 使用格式提示和约束
- ✅ 使用枚举限制可选值
- ✅ 明确标记必填字段

#### 12.2.2 单一职责原则（L1-A001.12.2.2）

每个工具应该只做一件事，并且做到最好。复杂操作应通过组合多个简单工具实现。

**反模式**：
```yaml
{
  "name": "manage_customer",
  "description": "创建、更新、删除或查询客户"  # ❌ 职责过重
}
```

**正模式**：
```yaml
[
  { "name": "create_customer", "description": "创建新客户记录" },
  { "name": "update_customer", "description": "更新现有客户信息" },
  { "name": "delete_customer", "description": "删除客户记录" },
  { "name": "get_customer", "description": "按ID获取客户信息" },
  { "name": "search_customers", "description": "按条件搜索客户" }
]
```

**优势**：
- 更清晰的模型决策
- 更简单的参数集
- 更容易测试
- 更可预测的行为

#### 12.2.3 参数类型严格化（L1-A001.12.2.3）

使用最严格的类型定义，避免模糊性。

| 避免 | 推荐 | 原因 |
|------|------|------|
| `string` 存储数字 | `number` 或 `integer` | 防止"five"替代5 |
| `string` 存储日期 | `string` + `format` | "2026-01-27" 而非 "下周二" |
| `string` 存储选项 | `enum` | 限制为有效值 |
| 开放 `object` | 明确属性定义 | 可预测结构 |

#### 12.2.4 结构化返回原则（L1-A001.12.2.4）

工具返回结果应结构化、可操作，便于模型理解和后续处理。

**反模式**：
```yaml
{ "result": "Customer created successfully" }  # ❌ 信息不足
```

**正模式**：
```yaml
{
  "success": true,
  "customer_id": "cust_abc123",
  "created_at": "2026-01-27T10:00:00Z",
  "action_taken": "create",
  "warnings": [],
  "next_steps": ["Add payment method", "Set subscription tier"]
}
```

**结构化返回的价值**：
- 便于模型确认操作成功
- 便于传递ID给后续工具调用
- 便于处理边缘情况
- 便于提供有意义的后续操作建议

### 12.3 工具调用流程（L1-A001.12.3）

```
用户请求 → 分析需求 → 工具选择 → 参数构建 → 调用执行 → 结果解析 → 最终响应
              ↓              ↓              ↓
          确定是否需要工具   选择最优工具   验证参数有效性
```

#### 12.3.1 工具选择决策树（L1-A001.12.3.1）

1. **是否需要外部信息？** → 使用搜索/查询工具
2. **是否需要修改外部状态？** → 使用写操作工具
3. **是否需要计算？** → 使用计算器/代码执行工具
4. **是否需要专业知识？** → 使用领域专家工具
5. **以上都不是？** → 直接回答

#### 12.3.2 参数验证流程（L1-A001.12.3.2）

```python
def validate_parameters(tool_name, params):
    # 1. 类型检查
    if not isinstance(params.get('query'), str):
        return {"valid": False, "error": "query must be string"}
    
    # 2. 范围检查
    count = params.get('count', 5)
    if not 1 <= count <= 10:
        return {"valid": False, "error": "count must be 1-10"}
    
    # 3. 必填检查
    if not params.get('query'):
        return {"valid": False, "error": "query is required"}
    
    # 4. 格式检查（邮箱、URL等）
    
    return {"valid": True}
```

### 12.4 工具安全与错误处理（L1-A001.12.4）

#### 12.4.1 幂等性设计（L1-A001.12.4.1）

所有写操作工具必须设计为幂等的，确保重试不会产生副作用。

**幂等操作示例**：
- ✅ `update_customer(customer_id, data)` - 相同输入产生相同结果
- ✅ `set_user_role(user_id, role)` - 重复调用结果一致

**非幂等操作处理**：
- 使用唯一请求ID防止重复执行
- 记录操作日志用于去重
- 在工具描述中明确说明非幂等性

#### 12.4.2 错误处理规范（L1-A001.12.4.2）

工具应返回结构化的错误信息，便于模型理解和恢复。

```yaml
{
  "success": false,
  "error_code": "NETWORK_ERROR",
  "error_message": "网络连接超时，请稍后重试",
  "retryable": true,
  "retry_after": 30,
  "suggestion": "请检查网络连接或30秒后重试"
}
```

**错误类型分类**：
| 错误类型 | 是否可重试 | 处理建议 |
|---------|----------|---------|
| 网络错误 | 是 | 指数退避重试 |
| 速率限制 | 是 | 等待指定时间后重试 |
| 参数错误 | 否 | 修正参数后重新调用 |
| 权限错误 | 否 | 提示用户提供权限 |
| 资源不存在 | 否 | 提示用户资源不存在 |

#### 12.4.3 超时控制（L1-A001.12.4.3）

所有工具调用必须设置超时限制，防止无限等待。

```python
async def execute_with_timeout(tool, params, timeout_seconds=30):
    try:
        result = await asyncio.wait_for(
            tool.execute(**params),
            timeout=timeout_seconds
        )
        return result
    except asyncio.TimeoutError:
        return {
            "success": False,
            "error_code": "TIMEOUT",
            "error_message": f"操作超时（{timeout_seconds}秒）"
        }
```

### 12.5 工具管理与版本控制（L1-A001.12.5）

#### 12.5.1 工具注册表（L1-A001.12.5.1）

维护中心化的工具注册表，记录所有可用工具及其元数据。

```yaml
{
  "tools": [
    {
      "id": "tool-001",
      "name": "search_web",
      "version": "1.0.0",
      "description": "搜索互联网获取实时信息",
      "category": "information",
      "schema": {...},
      "enabled": true,
      "usage_count": 1250
    }
  ]
}
```

#### 12.5.2 版本管理（L1-A001.12.5.2）

- 使用语义版本控制（Major.Minor.Patch）
- 向后兼容的变更使用Minor版本
- 破坏性变更使用Major版本
- 提供工具版本迁移指南

#### 12.5.3 工具使用监控（L1-A001.12.5.3）

监控指标：
| 指标 | 说明 | 阈值 |
|------|------|------|
| 调用成功率 | 成功调用/总调用 | >95% |
| 平均响应时间 | 工具执行耗时 | <500ms |
| 错误率 | 错误调用/总调用 | <5% |
| 使用频率 | 工具被调用次数 | 用于优化优先级 |

### 12.6 工具优先决策矩阵（L1-A001.12.6）

| 用户请求类型 | 工具优先行动 | 直接回答条件 |
|------------|-------------|-------------|
| 事实查询 | 使用搜索工具 | 高度确定的常识 |
| 数据查询 | 使用数据库工具 | 无 |
| 计算问题 | 使用计算器工具 | 简单算术 |
| 文件操作 | 使用文件工具 | 无 |
| 代码编写 | 使用代码工具 | 无 |
| 专业知识 | 使用专家工具 | 通用知识 |
| 闲聊对话 | 直接回答 | 所有情况 |
| 安全相关 | 使用安全工具 | 无 |

### 12.7 工具优先执行检查清单（L1-A001.12.7）

在执行任务前，检查以下项目：

- [ ] 是否有合适的工具可以完成此任务？
- [ ] 工具参数是否完整且有效？
- [ ] 是否理解工具的返回格式？
- [ ] 是否处理了可能的错误场景？
- [ ] 是否设置了合理的超时时间？
- [ ] 是否考虑了幂等性？

---

## 13. 蜂群模式与多智能体协作规则（L1-A001.13）

### 13.1 蜂群模式概述（L1-A001.13.1）

蜂群模式（Swarm Mode）是多智能体协作的去中心化模式，灵感来自蚁群、蜂群等生物群体的自组织行为。核心特征：

| 特征 | 说明 |
|------|------|
| 去中心化 | 无全局控制器，每个Agent独立决策 |
| 局部交互 | Agent只与邻居/相关方通信 |
| 自组织 | 宏观行为从微观交互中涌现 |
| 鲁棒性 | 单个Agent失败不导致系统崩溃 |

**适用场景**：
- 复杂任务需要多样化专业知识
- 需要并行处理提高吞吐量
- 任务边界不清晰需动态协作
- 需要系统具备故障容错能力

### 13.2 协作模式分类（L1-A001.13.2）

| 模式 | 结构 | 适用场景 | 代表框架 |
|------|------|---------|---------|
| **同级协作** | 同等地位，协商共识 | 分布式决策、投票场景 | Hivemoot Colony |
| **层级协作** | Manager→Worker | 任务分解分配 | CrewAI Crew, LangGraph Manager |
| **群峰模式** | 大量微型Agent自主组织 | 工业物联网、安全监控 | IIoT DMAS |
| **编排-工作者** | 中心协调器+执行者 | 明确任务边界 | AutoGen Group Chat |

### 13.3 CrewAI蜂群协作最佳实践（L1-A001.13.3）

#### 13.3.1 角色定义规则（L1-A001.13.3.1）

**✅ 正确示例**：
```python
researcher = Agent(role="Market Research Analyst", goal="Discover market trends", backstory="Expert analyst...")
writer = Agent(role="Technical Content Writer", goal="Create clear documentation", backstory="Professional writer...")
```

**❌ 避免示例**：
```python
agent1 = Agent(role="General Assistant", ...)  # 角色模糊
agent2 = Agent(role="Helper", ...)            # 职责重叠
```

**角色设计原则**：
- 每个角色有明确的专业身份和目标
- 角色之间技能互补，无职责重叠
- 角色背景故事塑造行为模式

#### 13.3.2 任务分配规则（L1-A001.13.3.2）

| 规则 | 说明 |
|------|------|
| 任务描述精确 | 避免模糊描述，直接指导协作 |
| 上下文传递 | 使用`context=[previous_task]`传递上游输出 |
| 期望输出明确 | 定义清晰的成功标准 |
| 依赖关系清晰 | 明确任务执行顺序 |

**JSON数据流转原则**：
- 避免多余的键值嵌套（如`output`外层包裹）
- 使用文件路径代替大数据内容传递
- JSON结构应简洁明了

#### 13.3.3 工具分配规则（L1-A001.13.3.3）

| 规则 | 说明 |
|------|------|
| 工具对齐 | Researcher配搜索工具，分析师配计算工具 |
| 按需分配 | 只分配任务真正需要的工具 |
| 避免冗余 | 不必要工具会造成困惑 |

#### 13.3.4 流程类型（L1-A001.13.3.4）

| 流程类型 | 说明 | 代码示例 |
|---------|------|---------|
| **Sequential** | 顺序执行，按依赖链传递 | `process=Process.sequential` |
| **Hierarchical** | 层级管理，Manager分配任务 | `process=Process.hierarchical` |
| **Parallel** | 并行执行，独立任务同时跑 | `agents=[...], tasks=[...]` |

### 13.4 通信协议规则（L1-A001.13.4）

| 协议 | 说明 | 生态 |
|------|------|------|
| **A2A** | Agent-to-Agent，Linux Foundation背书 | 50+伙伴，快速增长 |
| **MCP** | Model Context Protocol，工具调用标准化 | 成熟生态 |
| **Direct API** | 简单直接，缺乏标准化 | 早期方案 |
| **Shared Memory** | 共享内存通信，同进程内多Agent | 特定框架 |

### 13.5 共识机制规则（L1-A001.13.5）

| 机制 | 适用场景 | 实现方式 |
|------|---------|---------|
| **投票机制** | 提案评审、集体决策 | 过半数通过 |
| **协商机制** | 多方案辩论、权衡利益 | Agent之间论据辩论 |
| **优先级机制** | 层级协作 | Manager拥有最终决定权 |

### 13.6 任务分配策略（L1-A001.13.6）

| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **静态分配** | 执行前预先分配 | 简单确定 | 无法适应动态 |
| **动态分配** | 根据状态能力动态分配 | 灵活性高 | 协调开销大 |
| **市场机制** | Agent竞标获取任务 | 资源优化 | 计算开销 |

### 13.7 协作质量保障（L1-A001.13.7）

| 规则 | 说明 |
|------|------|
| **互补技能设计** | 团队技能互补，填充彼此空白 |
| **背景故事塑造** | Backstory不仅调味，影响实际行为 |
| **异常处理健壮** | 工具脚本完善异常处理+断点续做 |
| **小功能先验证** | 验证后扩展，避免级联错误 |

### 13.8 协作状态定义（L1-A001.13.8）

| 状态 | 图标 | 含义 |
|------|------|------|
| 初始化 | 🔧 | 蜂群正在初始化 |
| 任务分发 | 📤 | 任务正在分配给Agent |
| 执行中 | ⚙️ | Agent正在执行任务 |
| 等待中 | ⏳ | 等待其他Agent完成 |
| 协作中 | 🔄 | Agent之间正在通信 |
| 完成 | ✅ | 蜂群任务完成 |
| 失败 | ❌ | 蜂群执行失败 |

### 13.9 蜂群模式与Crews对比（L1-A001.13.9）

| 特性 | Crews（团队模式） | Flows（流程模式） |
|------|------------------|-----------------|
| **核心理念** | 自主协作，智能组合 | 事件驱动，精确控制 |
| **决策方式** | Agent间自然自主决策 | 预定义执行路径 |
| **适用场景** | 复杂多阶段任务 | 精确控制的生产流程 |
| **状态管理** | 动态协作产生 | 安全的内部状态管理 |
| **灵活性** | 高，动态问题解决 | 低，结构化流程 |

**最佳实践**：结合Crews和Flows，利用两者的协同效应构建生产级应用。

---

## 14. 自动检查与归类规则（L1-A001.14）

### 14.1 自动检查规则（L1-A001.14.1）

| 检查项 | 执行频率 | 触发条件 |
|--------|---------|---------|
| 文件编号唯一性 | 每次保存 | 保存时 |
| 层级归属正确性 | 每次保存 | 保存时 |
| 依赖关系完整性 | 每天 | 定时检查 |
| 版本一致性 | 每次加载 | 加载时 |

### 14.2 每日设计原则合规检查（L1-A001.14.2）

#### 14.2.1 检查目标（L1-A001.14.2.1）
每日自动检查所有规则文档是否符合面向对象分层架构设计原则：
- **L1层**：仅包含抽象定义和契约，无具体实现细节
- **L2层**：仅包含配置和约束定义，无具体代码实现
- **L3层**：仅包含具体实现代码，无抽象规范定义

#### 14.2.2 检查内容（L1-A001.14.2.2）

| 检查维度 | 检查规则 | 违规级别 |
|---------|---------|---------|
| **层级边界** | L1层不应包含具体数值配置（如 `sync_interval: 60`） | 严重 |
| **层级边界** | L2层不应包含具体代码实现 | 严重 |
| **层级边界** | L3层不应包含架构设计规范 | 警告 |
| **编号规范** | 所有文件必须有唯一编号标识 | 严重 |
| **编号规范** | 编号格式必须符合 `L{层级}-{类型}{序号}` | 警告 |
| **依赖关系** | L3→L2→L1 必须可追溯 | 严重 |
| **内容归属** | 架构设计内容必须在L2层 | 警告 |
| **内容归属** | 代码实现必须在L3层 | 警告 |

#### 14.2.3 检查执行（L1-A001.14.2.3）

**执行时间**：每天凌晨 2:00 自动执行

**执行流程**：
1. 扫描所有规则文件（`.trae/rules/**/*.md`）
2. 检查文件编号是否存在且唯一
3. 验证层级归属是否正确
4. 检测跨层级内容越界问题
5. 生成合规性报告
6. 发送告警通知（如有违规）

#### 14.2.4 违规处理（L1-A001.14.2.4）

| 违规级别 | 处理方式 |
|---------|---------|
| 严重 | 立即通知管理员，暂停相关规则生效 |
| 警告 | 生成报告，建议修复 |

#### 14.2.5 合规性报告格式（L1-A001.14.2.5）

```
========================================
规则文档合规性检查报告
日期: YYYY-MM-DD
========================================

【检查结果】
总文件数: 25
合规文件: 23
违规文件: 2

【违规详情】
1. .trae/rules/base/example.md
   - 问题: L3层包含架构设计内容
   - 级别: 警告
   - 建议: 移至L2层

2. .trae/rules/algorithm/utils.md
   - 问题: 缺少编号标识
   - 级别: 严重
   - 建议: 添加 identifier 字段

【合规率】: 92%
【状态】: ✅ 通过 / ⚠️ 警告 / ❌ 失败
========================================
```

### 14.3 自动归类规则（L1-A001.14.3）

| 规则 | 条件 | 动作 |
|------|------|------|
| 智能体归类 | `*_agent.md` | → `L3-C00x` |
| 规则归类 | `*.md` in rules/ | → `L3-R00x` |
| 技能归类 | `*/SKILL.md` | → `L3-S00x` |
| 算法归类 | `algorithms/*.py` | → `L3-M00x` |

### 14.4 目录优化结构（L1-A001.14.4）

```
.trae/
├── agents/          # L3-C001~C019 智能体
├── rules/           # L3-R001~R024 规则（按类别分）
│   ├── base/        # 基础规则
│   ├── algorithm/   # 算法规则
│   ├── skill/       # 技能规则
│   └── workflow/    # 工作流规则
├── skills/          # L3-S001~S002 技能
├── algorithms/      # L3-M001~M006 算法模块
└── workflows/       # L3-W00x 工作流
```

### 14.5 自动更新机制（L1-A001.14.5）

- **新增文件**: 自动分配编号 → 通知注册中心
- **删除文件**: 自动释放编号 → 更新依赖关系

---

## 15. 强制规则：只使用 .trae 规则体系（L1-A001.15）

### 15.1 三套体系独立（L1-A001.15.1）

| 规则体系 | 路径 | 用途 | 处理方式 |
|---------|------|------|---------|
| **使用** | `.trae/` | 主规则体系 | ✅ 只使用此体系 |
| **忽略** | `.opencode/` | 其他规则体系 | ❌ 完全忽略，不碰任何文件 |
| **忽略** | `新建文件夹/` | 历史版本存档 | ❌ 完全忽略，不碰任何文件 |

### 15.2 绝对不能碰的文件（L1-A001.15.2）

- 任何 `.opencode/` 目录下的文件
- 任何 `新建文件夹/` 目录下的文件
- 不要读取、不要修改、不要创建

### 15.3 只使用的文件（L1-A001.15.3）

- 所有 `.trae/` 目录下的文件
- 根目录的 `agent.md`（L1层）

### 15.4 为什么忽略新建文件夹（L1-A001.15.4）

- `新建文件夹/` 只用于历史版本存档
- 里面的内容可能过时或混乱
- 避免逻辑混乱和规则冲突
- 所有规范只从 `.trae/` 体系获取

---

## 16. 中央调度引擎双模式执行（L1-A001.16）

### 16.1 双模式架构（L1-A001.16.1）

| 模式 | 默认 | 说明 | 组件 |
|------|------|------|------|
| 同步模式 | ✅ 默认 | AgentCallInterface 直接调用 | 强耦合，低延迟 |
| 异步模式 | 可选 | MessageBus 消息总线 | 高吞吐量，容错 |

### 16.2 强耦合架构（L1-A001.16.2）

DispatcherAgent 与 TRAE 核心组件深度集成：
- AgentRegistry：智能体注册中心（直接引用）
- AgentCallInterface：同步调用接口
- MessageBus：异步消息总线

### 16.3 执行流程（L1-A001.16.3）

```
用户任务 → 博弈模式检测 → 智能体选择
                           ↓
              ┌────────────┴────────────┐
              ↓                         ↓
        同步模式                   异步模式
     (AgentCallInterface)       (MessageBus)
              ↓                         ↓
              └────────────┬────────────┘
                           ↓
                      结果聚合
```

---

## 17. 版本信息（L1-A001.17）

| 项目 | 值 |
|------|-----|
| **版本** | 1.5 |
| **日期** | 2026-05-13 |
| **同步状态** | ✅ 已与 L2/L3 同步 |
| **Cloud Code整合** | ✅ 四大精髓已集成 |
| **中央调度引擎** | ✅ 双模式执行（同步/异步） |
| **蜂群协作** | ✅ 去中心化决策、信息素传递、自适应行为 |

---

## 15. Cloud Code记忆系统核心改进（L1-A001.15）

### 15.1 四大核心精髓（L1-A001.15.1）

基于Cloud Code记忆系统的深度调研，提取以下四大核心精髓：

| 精髓 | 说明 | 优先级 |
|------|------|-------|
| **主动整理** | Dream机制：≥24小时且≥5会话自动触发整合、剪枝、更新 | P0 |
| **智能检索** | 小模型检索员：对话前注入前3-5条相关记忆 | P0 |
| **严谨验证** | 使用前验证："记忆说X存在"不等于"X现在存在" | P0 |
| **质量把控** | 准入过滤：只记录无法从代码/Git历史推导的信息 | P0 |

### 15.2 Dream整合机制（L1-A001.15.2）

#### 15.2.1 触发条件（L1-A001.15.2.1）
- 距上次整理 ≥24小时
- 期间工作会话数 ≥5个

#### 15.2.2 操作流程（L1-A001.15.2.2）
```
1. 扫描 → 读取记忆文件及会话日志
2. 整合 → 合成为持久、组织良好的记忆
3. 剪枝 → 删除过时、矛盾或冗余的记忆
4. 更新 → 直接写入记忆目录
```

#### 15.2.3 整合目标（L1-A001.15.2.3）
- 搞清楚自己现在知道什么
- 将相对时间转为绝对时间
- 新发现覆盖旧矛盾
- 保持记忆库的精炼和可用

### 15.3 智能检索员（L1-A001.15.3）

#### 15.3.1 检索机制（L1-A001.15.3.1）
- 在每次对话前，扫描记忆索引
- 根据相关性返回前3-5条记忆注入上下文

#### 15.3.2 相关性排序（L1-A001.15.3.2）
- 使用向量相似度计算
- 结合时间衰减因子
- 优先返回近期使用的记忆

### 15.4 记忆验证机制（L1-A001.15.4）

#### 15.4.1 验证规则（L1-A001.15.4.1）
| 记忆内容 | 验证方式 |
|---------|---------|
| 文件路径 | ls命令检查 |
| 函数参数 | grep确认 |
| 配置值 | 读取配置文件 |

#### 15.4.2 验证原则（L1-A001.15.4.2）
> **"记忆说X存在"不等于"X现在存在"**

### 15.5 记忆准入过滤（L1-A001.15.5）

#### 15.5.1 核心原则（L1-A001.15.5.1）
**只记录无法从代码和Git历史中推导出的信息**

#### 15.5.2 不记录内容清单（L1-A001.15.5.2）
| 类型 | 原因 | 替代方案 |
|------|------|---------|
| 代码模式 | 可通过grep获取 | grep命令 |
| 文件结构 | 可通过ls获取 | ls命令 |
| Git历史 | git log是权威来源 | git log |
| CLAUDE.md内容 | 避免重复 | 直接引用 |
| 临时任务状态 | 会话级信息无需持久化 | 上下文管理 |

### 15.6 记忆过期策略（L1-A001.15.6）

| 类型 | 过期阈值 | 处理方式 |
|------|---------|---------|
| User | 90天未访问 | 标记待审核 |
| Feedback | 180天未访问 | 自动归档 |
| Project | 365天未访问 | 标记待审核 |
| Reference | 无限期 | 手动清理 |

---

## 16. 三层同步状态（L1-A001.16）

| 层级 | 路径 | 版本 | 同步状态 |
|------|------|------|---------|
| L1 | `/agent.md` | 1.4 | ✅ |
| L2 | `/.trae/agent.md` | 2.9 | ✅ 已同步 |
| L3 | `/.trae/rules/*` | 2.9 | ✅ 已同步 |

**同步时间**: 2026-05-10T12:00:00Z
**Cloud Code整合状态**: ✅ 四大精髓已集成
- **修改文件**: 自动检查兼容性 → 触发版本更新

---

## 15. 智能体模块化组合系统（L1-A001.15）

### 15.1 核心概念（L1-A001.15.1）

**模块化组合系统**（Modular Composition System）是一个允许智能体动态引用和组合L3层各功能模块的规则管理框架。

**核心思想**：
- 将智能体能力拆分为独立的功能模块（技能、规则、算法、工作流）
- 通过声明式配置组合多个模块创建新智能体
- 模块之间通过标准接口通信，实现松耦合

**设计原则**：
| 原则 | 说明 |
|------|------|
| 模块化 | 每个模块职责单一，可独立开发和测试 |
| 可组合 | 模块可以自由组合，形成新的智能体 |
| 可替换 | 同类模块可互相替换，不影响整体功能 |
| 可扩展 | 新模块可无缝接入系统 |

### 15.2 模块类型定义（L1-A001.15.2）

#### 15.2.1 模块分类

| 模块类型 | 标识前缀 | 存储位置 | 说明 |
|---------|---------|---------|------|
| 规则模块 | L3-R | `.trae/rules/` | 业务规则、策略定义 |
| 技能模块 | L3-S | `.trae/skills/` | 可执行技能、工具调用 |
| 算法模块 | L3-M | `.trae/algorithms/` | 算法实现、计算逻辑 |
| 工作流模块 | L3-W | `.trae/workflows/` | 流程编排、任务调度 |
| 智能体模块 | L3-C | `.trae/agents/` | 预定义智能体模板 |

#### 15.2.2 模块元数据结构

```json
{
  "module_id": "L3-R025",
  "name": "工具优先原则",
  "version": "1.0.0",
  "type": "rule",
  "description": "智能体工具调用的核心原则",
  "dependencies": ["L3-R001", "L3-S001"],
  "interfaces": {
    "inputs": ["task_description", "agent_context"],
    "outputs": ["decision_result", "tool_selection"]
  },
  "enabled": true,
  "tags": ["tool", "priority", "core"]
}
```

### 15.3 智能体组合机制（L1-A001.15.3）

#### 15.3.1 组合配置格式

```json
{
  "agent_id": "custom_agent_001",
  "name": "数据分析智能体",
  "description": "组合数据查询和可视化能力的智能体",
  "modules": [
    {
      "module_id": "L3-S001",
      "alias": "data_fetcher",
      "config": {"timeout": 30}
    },
    {
      "module_id": "L3-M003",
      "alias": "data_analyzer",
      "config": {"algorithm": "regression"}
    },
    {
      "module_id": "L3-S002",
      "alias": "visualizer",
      "config": {"chart_type": "line"}
    }
  ],
  "workflow": {
    "type": "sequential",
    "steps": [
      {"module": "data_fetcher", "output_to": "data_analyzer.input"},
      {"module": "data_analyzer", "output_to": "visualizer.input"},
      {"module": "visualizer", "output_to": "result"}
    ]
  },
  "memory": {"type": "short_term", "ttl": 3600}
}
```

#### 15.3.2 模块间通信协议

| 协议要素 | 说明 |
|---------|------|
| 数据格式 | JSON结构，支持基本类型和嵌套对象 |
| 消息传递 | 通过事件总线或直接调用 |
| 错误处理 | 每个模块返回标准错误格式 |
| 超时控制 | 每个模块可配置独立超时 |

### 15.4 模块引用语法（L1-A001.15.4）

#### 15.4.1 绝对引用

```
@module(L3-R025)          // 引用特定模块
@module(L3-R025@1.0.0)    // 引用特定版本
@module(L3-R025@latest)   // 引用最新版本
```

#### 15.4.2 相对引用

```
@module(./local_module.md)        // 相对路径引用
@module(../skills/my_skill.md)   // 上级目录引用
```

#### 15.4.3 动态引用

```
@module(tag:security)       // 引用所有带security标签的模块
@module(type:skill)         // 引用所有技能模块
@module(pattern:L3-S*)      // 通配符匹配
```

### 15.5 组合执行流程（L1-A001.15.5）

```
用户请求
    │
    ▼
┌─────────────────────────────────────────┐
│ 1. 智能体解析器                          │
│    解析组合配置，识别依赖模块              │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ 2. 模块加载器                            │
│    按依赖顺序加载所有模块                  │
│    检查版本兼容性                        │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ 3. 工作流引擎                            │
│    按配置的流程执行模块                  │
│    处理模块间数据传递                    │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ 4. 结果聚合器                            │
│    收集所有模块输出                      │
│    生成最终响应                          │
└─────────────────────────────────────────┘
    │
    ▼
用户响应
```

### 15.6 模块生命周期管理（L1-A001.15.6）

#### 15.6.1 生命周期阶段

| 阶段 | 触发时机 | 执行操作 |
|------|---------|---------|
| 初始化 | 智能体创建时 | 加载模块元数据，建立依赖关系 |
| 激活 | 首次执行前 | 初始化模块内部状态 |
| 执行 | 收到任务时 | 执行模块逻辑，返回结果 |
| 挂起 | 长时间空闲 | 保存状态，释放资源 |
| 销毁 | 智能体删除时 | 清理资源，释放内存 |

#### 15.6.2 状态管理

```json
{
  "agent_id": "custom_agent_001",
  "status": "active",
  "modules": {
    "L3-S001": {"status": "running", "last_used": "2026-01-27T10:00:00Z"},
    "L3-M003": {"status": "idle", "last_used": "2026-01-27T09:30:00Z"}
  },
  "resources": {"memory_usage": 128, "cpu_usage": 15}
}
```

### 15.7 依赖管理与版本控制（L1-A001.15.7）

#### 15.7.1 依赖声明

```json
{
  "dependencies": {
    "L3-R001": "^1.0.0",    // 兼容1.x.x版本
    "L3-S001": "~2.1.0",    // 兼容2.1.x版本
    "L3-M001": "3.0.0",     // 精确版本
    "L3-W001": ">=4.0.0"    // 最低版本要求
  }
}
```

#### 15.7.2 版本冲突解决策略

| 冲突类型 | 解决策略 |
|---------|---------|
| 版本不兼容 | 使用指定版本或提示用户 |
| 循环依赖 | 检测并拒绝加载 |
| 缺失依赖 | 自动下载或报错 |
| 依赖冲突 | 提示用户手动选择 |

### 15.8 模块化智能体创建检查清单（L1-A001.15.8）

| 检查项 | 说明 |
|--------|------|
| 模块职责清晰 | 每个模块只做一件事 |
| 接口定义明确 | 输入输出格式规范 |
| 依赖关系明确 | 声明所有外部依赖 |
| 版本控制 | 使用语义版本管理 |
| 错误处理 | 提供完整的错误处理机制 |
| 文档完备 | 包含使用说明和示例 |
| 测试覆盖 | 模块有单元测试 |
| 性能指标 | 定义性能基准 |

---

**文档版本**: v1.4 | **最后更新**: 2026-05-08 | **章节数**: 15